#pragma once

namespace GameConstants {
    const int SCREEN_WIDTH = 800;
    const int SCREEN_HEIGHT = 600;
    const float BASE_CELL_SIZE = 30.0f;
    const float MINIMAP_SCALE = 0.5f;
    const float POWERUP_DURATION = 10.0f;
}